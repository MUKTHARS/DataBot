    # API Configuration
    API_HOST=0.0.0.0
    API_PORT=8000
    DEBUG=true

OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
OPENAI_MAX_TOKENS=2000 
OPENAI_TEMPERATURE=0.1

    # Database Configuration (set based on your needs)
    DATABASE_TYPE=mongodb

    POSTGRES_URL=postgresql+asyncpg://postgres:password@localhost:5432/analytics_db

    
    MYSQL_URL=mysql://username:password@localhost:3306/analytics_db

   
    MONGODB_URL=mongodb://localhost:27017/analytics_db

    # Redis Cache (optional)
    REDIS_URL=redis://localhost:6379/0
    CACHE_TTL=300

    # Security
    CORS_ORIGINS=http://localhost:3000,http://localhost:5173
    RATE_LIMIT_PER_MINUTE=60

    # Logging
    LOG_LEVEL=INFO

    # Agent Configuration
    AGENT_SYSTEM_PROMPT=You are a sophisticated data analytics assistant with access to a live database. You can understand natural language queries about data, generate appropriate SQL or MongoDB queries, analyze results, and provide insights. Always verify query safety before execution and never execute destructive operations.  
